# render.yaml — Render Blueprint (optional, can configure via dashboard instead)
services:
  - type: web
    name: citizen-monitor
    runtime: node
    buildCommand: npm install
    startCommand: node src/server.js
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
      - key: VK_APP_ID
        sync: false
      - key: VK_APP_SECRET
        sync: false
      - key: VK_BOT_WEBHOOK
        sync: false
